# Runtime dependencies, exact-pinned. Upgrade deliberately, never automatically —
# see docs/OPERATIONS.md "Applying updates".
#
# Everything here is pure Python: the image needs no compiler. SQLite itself is not
# listed because it ships inside Python's standard library (the `sqlite3` module),
# linked against Debian's libsqlite3, which has FTS5 enabled. app/db.py proves that
# at startup rather than assuming it.

fastapi==0.141.1
uvicorn==0.52.3
jinja2==3.1.6
